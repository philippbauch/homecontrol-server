/**
 * Default name of the database to connect with.
 */
const DEFAULT_DB_NAME = "home.philippbauch.com";

/**
 * Default name of the directory to store the log files in.
 */
const DEFAULT_LOGS_DIR = "logs";

/**
 * Default port for the server to listen on.
 */
const DEFAULT_PORT = 8000;

module.exports = {
  DEFAULT_DB_NAME,
  DEFAULT_LOGS_DIR,
  DEFAULT_PORT
};
